# Frontend Environment Variables

# API Base URL (use relative path for same-origin, or full URL for different domain)
# For Vercel deployment, use relative path: /api
# For local development with separate backend, use: http://localhost:8000
NEXT_PUBLIC_API_BASE=/api

# Backend URL (only used in Next.js API routes, not exposed to client)
# Set this in Vercel environment variables
BACKEND_URL=http://localhost:8000
